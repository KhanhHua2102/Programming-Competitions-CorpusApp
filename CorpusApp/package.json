{
  "name": "corpusapp",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install && concurrently \"cd client && npm install\" \"cd server && npm install\"",
    "start": "concurrently \"cd client && npm run prod\" \"cd server && npm run prod\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
